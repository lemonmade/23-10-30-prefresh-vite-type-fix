Reproduces [this `@prefresh/vite` issue](https://github.com/preactjs/prefresh/issues/518). To see the type error described in that issue, run the following commands:

```bash
pnpm install
pnpm start
```
